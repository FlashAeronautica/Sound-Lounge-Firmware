# Sound Lounge Firmware (Flash Aeronautica)

This repository is a **test / archive** mirror. It does **not** use the same folder layout as Sala Frequencies production OTA.

| Path | Purpose |
|------|---------|
| `next/` | Test builds (install / point tools here) |
| `production/<version>/` | Archived production packages |

**Production OTA for devices:** [Sala-Frequencies/Sound-Lounge-Firmware](https://github.com/Sala-Frequencies/Sound-Lounge-Firmware) (repo root `manifest.txt`).

Test manifest: `next/manifest.txt`  
Latest archived production: `production/` (versioned subfolders)